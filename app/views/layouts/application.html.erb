<!DOCTYPE html>
<html>
<head>
  <title>ClioInOutStub</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

  <% flash.to_hash.slice(:alert, :error, :notice).each_pair do |type, messages| %>
    <% Array(messages).each do |m| %>
      <p class="<%= type %>"><%= m %></p>
    <% end %>
  <% end %>

  <%= yield %>

</body>
</html>
